{
  "name": "ProBuzzer",
  "version": "0.1.0",
  "description": "Virtual soundboard that plays sounds in the browser",
  "homepage": "./",
  "scripts": {
    "prebuild": "rm -rf dist",
    "start": "parcel index.html --open chrome",
    "build": "parcel build index.html --no-source-maps",
    "postbuild": "sh ./replace.sh"
  },
  "license": "MIT",
  "dependencies": {
    "axios": "^0.21.1",
    "react": "^16.13.1",
    "react-dom": "^16.13.1"
  },
  "devDependencies": {
    "babel-preset-nano-react-app": "^0.1.0",
    "parcel": "^2.0.0-alpha.3.2"
  },
  "babel": {
    "presets": [
      "nano-react-app"
    ]
  }
}
